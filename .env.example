// Configuration pour les variables d'environnement Expo
// Copier ce fichier vers .env et modifier les valeurs selon votre configuration

// IP de votre machine sur le réseau local
// Pour trouver votre IP: ifconfig | grep "inet " | grep -v 127.0.0.1
export const API_BASE_URL = process.env.EXPO_PUBLIC_API_URL || "http://20.20.20.204:8000/api"

// Socket.IO URL (si utilisé séparément)
export const SOCKET_URL = process.env.EXPO_PUBLIC_SOCKET_URL || "http://20.20.20.204:8000"

// Timeout des requêtes en millisecondes
export const API_TIMEOUT = 15000

// Configuration debug
export const DEBUG_MODE = __DEV__

// Clés pour le stockage local
export const STORAGE_KEYS = {
  AUTH_TOKEN: "auth_token",
  REFRESH_TOKEN: "refresh_token",
  USER_DATA: "user_data",
  USER_TYPE: "user_type",
}

